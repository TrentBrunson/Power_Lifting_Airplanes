-- make copies of tables for analysis with INTO
-- includes only table & column info; no kets, indexes, etc.

--SELECT select_list (permanent table that's NOT already in DB)
--INTO table_name
--FROM table_source
--[WHERE search_condition]
--[GROUP BY group_by_list]
--[HAVING search_condition]
--[ORDER BY order_by_list]

